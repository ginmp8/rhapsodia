{
  "target_skill": "{{target_skill}}",
  "status": "planned",
  "schema_version": "2.0",
  "scenarios": [
    {
      "id": "A001",
      "type": "should_activate",
      "prompt": "{{should_activate_prompt}}",
      "expected_behavior": "{{should_activate_expected_behavior}}",
      "acceptance_criteria": [
        "{{should_activate_acceptance_criterion}}"
      ]
    },
    {
      "id": "N001",
      "type": "should_not_activate",
      "prompt": "{{should_not_activate_prompt}}",
      "expected_behavior": "{{should_not_activate_expected_behavior}}",
      "acceptance_criteria": [
        "{{should_not_activate_acceptance_criterion}}"
      ]
    },
    {
      "id": "M001",
      "type": "ambiguous",
      "prompt": "{{ambiguous_prompt}}",
      "expected_behavior": "{{ambiguous_expected_behavior}}",
      "acceptance_criteria": [
        "{{ambiguous_acceptance_criterion}}"
      ]
    },
    {
      "id": "E001",
      "type": "edge_case",
      "prompt": "{{edge_case_prompt}}",
      "expected_behavior": "{{edge_case_expected_behavior}}",
      "acceptance_criteria": [
        "{{edge_case_acceptance_criterion}}"
      ]
    }
  ]
}
