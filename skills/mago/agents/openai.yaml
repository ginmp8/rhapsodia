interface:
  display_name: Mago
  short_description: Tech-lead planning for specs, PRD refinement, technical design,
    tasks, validation, and architecture decisions.
  default_prompt: Use $mago to refine governance intake into repository-aware planning
    artifacts, technical designs, task plans, validation plans, and architecture ADRs;
    never edit product code or runtime execution records.
  allow_implicit_invocation: true
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
