interface:
  display_name: Magia
  short_description: Senior engineering execution for bounded implementation, validation,
    hardening, technical docs, and implementation ADRs.
  default_prompt: Use $magia to implement, validate, debug, harden, document, and
    synchronize execution from current code and Mago specs without rewriting PRDs
    or governance artifacts.
  allow_implicit_invocation: true
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
